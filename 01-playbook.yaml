# playbook is list of plays always should start with -
- name: ping the node
  hosts: mongodb
  # this is the list of tasks/modules/collections
  tasks:
  - name: pinging the server
   # this isthe map
    ansible.builtin.ping: